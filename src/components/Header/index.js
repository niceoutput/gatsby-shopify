import React from 'react';
import { HeaderWrapper } from './styles';
import { Cart } from '../Cart';

export const Header = () => {
  return (
    <HeaderWrapper>
      <Cart />
    </HeaderWrapper>
  );
};
